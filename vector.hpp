#ifndef VECTOR_HPP
#define VECTOR_HPP

template <typename T>
class Vector
{
    // Your implementation of the Vector class starts here
};

template<typename T>
T dot(const Vector<T>& lhs, const Vector<T>& rhs)
{
    T d = 0;


}

#endif // VECTOR_HPP
